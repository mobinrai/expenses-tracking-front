const Header = () => {
    return (
        <header className="flex justify-between p-4">
            {/* <h3>Search</h3>
            <span className="">{user.name}</span> */}
        </header>
    )
}

export default Header